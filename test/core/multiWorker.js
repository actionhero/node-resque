var specHelper = require(__dirname + "/../_specHelper.js").specHelper;
var should = require('should');

describe('multiWorker', function(){

  it('should never have less than one worker')
  it('should stop adding workers when the max is hit')
  it('should not add workers when CPU utilization is high')
  it('should add workers when CPU utilization is low')
  it('should pass on all worker emits to the instance of multiWorker')

})